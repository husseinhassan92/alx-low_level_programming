#include <main.h>
/**
 *  mul - multiplies two integers
 * @a: one integer
 * @b: second integer
 * Return: product of a and b
 */
int mul(int a, int b)
{
	int a;
	int b;
	int mul;

	mul  = a * b;
	return (mul);
}
